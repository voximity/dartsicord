enum ChannelType {
  guildText,
  dm,
  guildVoice,
  groupDm,
  guildCategory
}

enum TokenType {
  bot,
  user
}

enum StatusType {
  online,
  doNotDisturb,
  away,
  invisible,
  offline
}

enum MessageDownloadType {
  before,
  after,
  around
}

enum ActivityType {
  game,
  stream,
  listen,
  watch
}